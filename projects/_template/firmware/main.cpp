// {Project name} — firmware skeleton
// Level: {…}
// Paired software: ../companion/README.md
//
// Build system: Arduino or PlatformIO (both work with this style).
// Board: {ESP32 / ESP32-S3 / ESP32-C3 / ...}

#include <Arduino.h>
// #include <Wire.h>
// #include <WiFi.h>

void setup() {
  Serial.begin(115200);
  // Init sensors, radios, displays.
}

void loop() {
  // 1) sense
  // 2) transform
  // 3) distill
  // 4) represent / do
  // 5) sleep (deep-sleep-first on battery builds)
  delay(1000);
}
